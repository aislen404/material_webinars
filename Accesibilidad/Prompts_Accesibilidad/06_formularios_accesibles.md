# Prompt — Formularios accesibles
Analiza el formulario y devuelve:
- Problemas de etiquetado (label/for, aria-describedby).
- Mensajes de error y anuncios a lector de pantalla.
- Gestión de foco tras el error.
Formulario:
```html
<!-- Pega formulario -->
```